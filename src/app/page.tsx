/*
 * Questo file definisce la pagina principale dell'app che mostra la dashboard manutenzioni.
 */

import { DashboardClient } from "@/components/dashboard-client";

/**
 * Renderizza la pagina dashboard principale con tabella e controlli di ricerca/ordinamento.
 */
export default function Home() {
  return <DashboardClient />;
}
